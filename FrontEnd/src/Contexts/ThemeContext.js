/**
 * useState context that manage the state either light or dark
 * @context ThemeContext
 * @type {React.Context}
 */
import { createContext } from 'react';

const ThemeContext = createContext();

export default ThemeContext;